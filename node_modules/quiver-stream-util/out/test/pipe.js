'use strict';

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { 'default': obj }; }

var _libStreamUtilJs = require('../lib/stream-util.js');

var _chai = require('chai');

var _chai2 = _interopRequireDefault(_chai);

var _chaiAsPromised = require('chai-as-promised');

var _chaiAsPromised2 = _interopRequireDefault(_chaiAsPromised);

_chai2['default'].use(_chaiAsPromised2['default']);
const should = _chai2['default'].should();

const buffers = ['one ', 'two ', 'three'];

describe('pipestream test', function () {
  it('should pipe successfully', function () {
    const sourceStream = (0, _libStreamUtilJs.buffersToStream)(buffers);

    var _createChannel = (0, _libStreamUtilJs.createChannel)();

    const writeStream = _createChannel.writeStream;
    const readStream = _createChannel.readStream;

    const p1 = (0, _libStreamUtilJs.pipeStream)(sourceStream, writeStream);

    const p2 = (0, _libStreamUtilJs.streamToText)(readStream).then(function (text) {
      return text.should.equal('one two three');
    });

    return Promise.all([p1, p2]);
  });

  it('should write fail', function () {
    var _createChannel2 = (0, _libStreamUtilJs.createChannel)();

    const writeStream1 = _createChannel2.writeStream;
    const readStream1 = _createChannel2.readStream;

    var _createChannel3 = (0, _libStreamUtilJs.createChannel)();

    const writeStream2 = _createChannel3.writeStream;
    const readStream2 = _createChannel3.readStream;

    const p1 = (0, _libStreamUtilJs.pipeStream)(readStream1, writeStream2).should.be.rejected;

    const p2 = readStream2.read().then(function (_ref) {
      let closed = _ref.closed;
      let data = _ref.data;

      data.should.equal('one');
      readStream2.closeRead(new Error());
    });

    writeStream1.write('one');
    const p3 = writeStream1.prepareWrite().should.be.rejected;

    return Promise.all([p1, p2, p3]);
  });

  it('should read fail', function () {
    var _createChannel4 = (0, _libStreamUtilJs.createChannel)();

    const writeStream1 = _createChannel4.writeStream;
    const readStream1 = _createChannel4.readStream;

    var _createChannel5 = (0, _libStreamUtilJs.createChannel)();

    const writeStream2 = _createChannel5.writeStream;
    const readStream2 = _createChannel5.readStream;

    const p1 = (0, _libStreamUtilJs.pipeStream)(readStream1, writeStream2).should.be.rejected;

    writeStream1.write('one');
    writeStream1.closeWrite(new Error());

    const p2 = readStream2.read().then(function (_ref2) {
      let closed = _ref2.closed;
      let data = _ref2.data;

      data.should.equal('one');

      return readStream2.read().should.be.rejected;
    });

    return Promise.all([p1, p2]);
  });
});