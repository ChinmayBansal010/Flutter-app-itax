'use strict';

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { 'default': obj }; }

function _asyncToGenerator(fn) { return function () { var gen = fn.apply(this, arguments); return new Promise(function (resolve, reject) { var callNext = step.bind(null, 'next'); var callThrow = step.bind(null, 'throw'); function step(key, arg) { try { var info = gen[key](arg); var value = info.value; } catch (error) { reject(error); return; } if (info.done) { resolve(value); } else { Promise.resolve(value).then(callNext, callThrow); } } callNext(); }); }; }

var _libStreamUtil = require('../lib/stream-util');

var _chai = require('chai');

var _chai2 = _interopRequireDefault(_chai);

var _chaiAsPromised = require('chai-as-promised');

var _chaiAsPromised2 = _interopRequireDefault(_chaiAsPromised);

_chai2['default'].use(_chaiAsPromised2['default']);
const should = _chai2['default'].should();

describe('stream pushback test', function () {
  it('should emit pushed back buffers first', _asyncToGenerator(function* () {
    const testBuffers = ['foo ', 'bar'];
    const pushbackBuffers = ['before1 ', 'before2 '];

    let readStream = (0, _libStreamUtil.buffersToStream)(testBuffers);
    readStream = (0, _libStreamUtil.pushbackStream)(readStream, pushbackBuffers);

    var _ref = yield readStream.peek();

    let data = _ref.data;

    data.should.equal('before1 ');
    var _ref2 = yield readStream.peek();

    data = _ref2.data;

    data.should.equal('before1 ');

    yield (0, _libStreamUtil.streamToText)(readStream).should.eventually.equal('before1 before2 foo bar');
  }));

  it('nested stream pushback test', _asyncToGenerator(function* () {
    const testBuffers = ['six ', 'seven'];
    let readStream = (0, _libStreamUtil.buffersToStream)(testBuffers);

    readStream = (0, _libStreamUtil.pushbackStream)(readStream, ['four ', 'five ']);
    readStream = (0, _libStreamUtil.pushbackStream)(readStream, ['three ']);
    readStream = (0, _libStreamUtil.pushbackStream)(readStream, ['one ', 'two ']);

    yield (0, _libStreamUtil.streamToText)(readStream).should.eventually.equal('one two three four five six seven');
  }));
});