'use strict';

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { 'default': obj }; }

var _chai = require('chai');

var _chai2 = _interopRequireDefault(_chai);

var _libEmpty = require('../lib/empty');

const should = _chai2['default'].should();

describe('empty stream test', function () {
  it('test empty read stream', function () {
    const readStream = (0, _libEmpty.emptyReadStream)();

    should.exist(readStream.isClosed());

    return readStream.read().then(function (_ref) {
      let closed = _ref.closed;
      let data = _ref.data;

      should.exist(closed);
      should.not.exist(data);
    });
  });

  it('test empty write stream', function () {
    const writeStream = (0, _libEmpty.emptyWriteStream)();

    writeStream.write('ignored data');
    should.exist(writeStream.isClosed());

    return writeStream.prepareWrite().then(function (_ref2) {
      let closed = _ref2.closed;

      should.exist(closed);
    });
  });
});