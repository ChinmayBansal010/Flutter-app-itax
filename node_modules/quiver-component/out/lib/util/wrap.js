"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});

var safePromised = require("quiver-promise").safePromised;

const assertFunction = function (fn) {
  if (typeof fn != "function") {
    throw new Error("argument must be of type function");
  }
};

const safeHandler = function (handler) {
  let options = arguments[1] === undefined ? {} : arguments[1];
  var _options$safeWrapped = options.safeWrapped;
  const safeWrapped = _options$safeWrapped === undefined ? false : _options$safeWrapped;

  if (safeWrapped) return handler;

  assertFunction(handler);
  options.safeWrapped = true;

  return safePromised(handler);
};

exports.safeHandler = safeHandler;
const safeBuilder = function (builder) {
  let options = arguments[1] === undefined ? {} : arguments[1];
  var _options$safeWrapped = options.safeWrapped;
  const safeWrapped = _options$safeWrapped === undefined ? false : _options$safeWrapped;

  if (safeWrapped) return builder;

  assertFunction(builder);
  options.safeWrapped = true;

  const wrappedBuilder = safePromised(builder);

  return function () {
    for (var _len = arguments.length, args = Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }

    return wrappedBuilder.apply(undefined, args).then(safeHandler);
  };
};
exports.safeBuilder = safeBuilder;