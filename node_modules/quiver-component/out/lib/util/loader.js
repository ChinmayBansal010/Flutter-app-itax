"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _quiverPromise = require("quiver-promise");

var resolve = _quiverPromise.resolve;
var reject = _quiverPromise.reject;

var _quiverSimpleHandler = require("quiver-simple-handler");

var streamToSimpleHandler = _quiverSimpleHandler.streamToSimpleHandler;
var validateSimpleTypes = _quiverSimpleHandler.validateSimpleTypes;

var getHandlerMap = require("./config").getHandlerMap;

const loadHandleable = function (config, componentId, builder) {
  const handlerMap = getHandlerMap(config);

  const handleable = handlerMap[componentId];
  if (handleable) return resolve(handleable);

  return builder(config).then(function (handleable) {
    if (!handleable) return reject(new Error("handleable is not defined in builder result"));

    handlerMap[componentId] = handleable;

    return handleable;
  });
};

exports.loadHandleable = loadHandleable;
const loadStreamHandler = function () {
  for (var _len = arguments.length, args = Array(_len), _key = 0; _key < _len; _key++) {
    args[_key] = arguments[_key];
  }

  return loadHandleable.apply(undefined, args).then(function (handleable) {
    const handler = handleable.streamHandler;
    if (!handler) return reject(new Error("handleable is not a stream handler"));

    return handler;
  });
};

exports.loadStreamHandler = loadStreamHandler;
const loadHttpHandler = function () {
  for (var _len = arguments.length, args = Array(_len), _key = 0; _key < _len; _key++) {
    args[_key] = arguments[_key];
  }

  return loadHandleable.apply(undefined, args).then(function (handleable) {
    const handler = handleable.httpHandler;
    if (!handler) return reject(new Error("handleable is not a http handler"));

    return handler;
  });
};

exports.loadHttpHandler = loadHttpHandler;
const loadSimpleHandler = function (config, componentId, builder, inType, outType) {
  const err = validateSimpleTypes([inType, outType]);
  if (err) return reject(err);

  return loadStreamHandler(config, componentId, builder).then(function (handler) {
    return streamToSimpleHandler(handler, inType, outType);
  });
};

exports.loadSimpleHandler = loadSimpleHandler;
const simpleHandlerLoader = function (inType, outType) {
  return function (config, componentId, builder) {
    return loadSimpleHandler(config, componentId, builder, inType, outType);
  };
};
exports.simpleHandlerLoader = simpleHandlerLoader;