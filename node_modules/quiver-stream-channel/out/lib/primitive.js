"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
const nextTick = process.nextTick;
const primitiveChannel = function () {
  let readStream = {};
  let writeStream = {};

  let mStreamClosed = null;

  let mReadCallback = null;
  let mWriteCallback = null;

  let mStreamInProgress = false;

  const dispatchCallback = function () {
    mStreamInProgress = true;
    let writerCalled = false;

    const writer = function (writeClosed, buffer) {
      if (writerCalled) throw new Error("writer can only be called once");

      writerCalled = true;

      if (!writeClosed && !buffer) throw new Error("Must supply either writeClosed or buffer");

      if (writeClosed && buffer) throw new Error("You may not supply a buffer while close the stream at the same time");

      if (writeClosed && !mStreamClosed) mStreamClosed = writeClosed;

      nextTick(function () {
        mStreamInProgress = false;
        const readCallback = mReadCallback;
        mReadCallback = null;

        readCallback(writeClosed, buffer);
      });
    };

    nextTick(function () {
      const writeCallback = mWriteCallback;
      mWriteCallback = null;

      writeCallback(null, writer);
    });
  };

  const dispatchIfReady = function () {
    if (mWriteCallback && mReadCallback && !mStreamInProgress) dispatchCallback();
  };

  const notifyStreamClosed = function (callback) {
    return nextTick(function () {
      mStreamInProgress = false;
      mReadCallback = null;
      mWriteCallback = null;

      callback(mStreamClosed);
    });
  };

  readStream.read = function (callback) {
    if (mReadCallback || mStreamInProgress) throw new Error("read called multiple times without waiting for callback");

    mReadCallback = callback;
    if (mStreamClosed) {
      notifyStreamClosed(callback);
    } else {
      dispatchIfReady();
    }
  };

  readStream.closeRead = function (err) {
    if (mReadCallback) throw new Error("cannot close read stream before read callback is compconsted");

    if (mStreamClosed) return;
    mStreamClosed = { err: err };

    if (mWriteCallback && !mStreamInProgress) {
      notifyStreamClosed(mWriteCallback);
    }
  };

  writeStream.prepareWrite = function (callback) {
    if (mWriteCallback) throw new Error("prepareWrite called multiple times without waiting for callback");

    mWriteCallback = callback;
    if (mStreamClosed) {
      notifyStreamClosed(callback);
    } else {
      dispatchIfReady();
    }
  };

  writeStream.closeWrite = function (err) {
    if (mWriteCallback) throw new Error("cannot close write stream before write callback is compconsted");

    if (mStreamClosed) return;
    mStreamClosed = { err: err };

    if (mReadCallback && !mStreamInProgress) notifyStreamClosed(mReadCallback);
  };

  const channel = {
    writeStream: writeStream,
    readStream: readStream
  };

  return channel;
};
exports.primitiveChannel = primitiveChannel;