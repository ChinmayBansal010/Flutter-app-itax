'use strict';

Object.defineProperty(exports, '__esModule', {
  value: true
});

var _copy = require('./copy');

const noCopy = function noCopy(object) {
  return object;
};

const merge = function merge(objects) {
  let options = arguments[1] === undefined ? {} : arguments[1];

  const copyObject = options.copy ? _copy.copy : noCopy;
  const mergedObject = {};

  objects.forEach(function (object) {
    for (let key in object) {
      mergedObject[key] = copyObject(object[key]);
    }
  });

  return mergedObject;
};
exports.merge = merge;