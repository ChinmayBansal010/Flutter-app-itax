export class Util {
    static clone(source:{}){
        return Object.assign({}, source); 
    }
    static cloneArray(source:any[]){
        return Object.assign([], source); 
    }
    static isEmptyObject(source:any){
        return Object.keys(source).length === 0; 
    }
}
